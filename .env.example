# Inference-as-a-Service - Local Development Environment Variables
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Fill in the values below with real secrets
# 3. Store the completed .env in 1Password (vault: IAS-Development)
# 4. NEVER commit .env to Git!
#
# To sync to another machine:
# eval $(op signin)
# op document get "IAS Local .env" --vault IAS-Development > .env

# ============================================
# Database (PostgreSQL)
# ============================================
DATABASE_URL=postgres://ias_user:CHANGEME@localhost:5432/ias_dev?sslmode=disable
DATABASE_MAX_CONNECTIONS=10

# ============================================
# Redis
# ============================================
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=CHANGEME

# ============================================
# RabbitMQ
# ============================================
RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# ============================================
# JWT Secret (generate: openssl rand -hex 32)
# ============================================
JWT_SECRET=CHANGEME_GENERATE_WITH_openssl_rand_hex_32
JWT_ACCESS_TTL=1h
JWT_REFRESH_TTL=168h

# ============================================
# Local Development
# ============================================
LOG_LEVEL=debug
ENABLE_DEBUG_ENDPOINTS=true
